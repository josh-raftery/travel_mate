export default interface Accomodation {
    destination_id: number;
    name: string;
    start_date: string;
    end_date: string;
    address: string;
    cost: number;
    currency: string;
    itinerary_id: number;
}